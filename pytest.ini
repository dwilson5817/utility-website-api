[pytest]
# Each Lambda is a top-level <tool>_handler/ dir holding its app package. Put
# those roots on the import path so tests can `import interrail` (add a line
# per new tool, e.g. weather_handler).
pythonpath = interrail_handler
